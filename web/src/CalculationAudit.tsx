import { useEffect, useMemo, useState } from 'react'
import './calculation-audit.css'

type PeriodOption = 'todo-periodo' | 'mes-atual' | 'mes-anterior' | 'personalizado'
type DetailKey = 'pedidos_fora_carteira'|'pedidos_sem_consultor'|'itens_sem_ean'|'itens_sem_produto'|'itens_sem_classificacao'|'datas_futuras'
type AuditResult = {id?:string;periodo:{inicio:string|null;fim:string|null};status:'ok'|'atencao'|'critico';total_alertas:number;executado_em:string;volume:{pedidos_faturados:number;itens_faturados:number;valor_total:number;primeira_data:string|null;ultima_data:string|null};conciliacao:{ol_total:number;ol_sem_combate:number;ol_combate:number;ol_prioritarios:number;ol_lancamentos:number;ol_sem_classificacao:number;diferenca:number};vinculos:{pedidos_sem_cnpj_vinculado:number;pedidos_fora_carteira:number;pedidos_sem_consultor:number;itens_sem_ean:number;itens_sem_produto:number;itens_sem_classificacao:number};qualidade:{clientes_sem_cnpj:number;clientes_sem_consultor:number;clientes_sem_uf:number;datas_invalidas:number;datas_futuras:number;duplicatas_pedidos:number;duplicatas_itens:number;pedidos_valor_divergente:number;itens_valor_negativo:number;valor_negativo:number;status_faturado_excluido:number;valor_status_excluido:number}}
type AuditHistory={id:string;criado_em:string;resultado:AuditResult};type Props={adminKey:string;enabled:boolean};type Detail={titulo:string;linhas:Array<Record<string,unknown>>}
const currency=new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}),number=new Intl.NumberFormat('pt-BR')
function localDate(date:Date){return `${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}-${String(date.getDate()).padStart(2,'0')}`}
function monthBounds(offset:number){const now=new Date();return{inicio:localDate(new Date(now.getFullYear(),now.getMonth()+offset,1)),fim:localDate(new Date(now.getFullYear(),now.getMonth()+offset+1,0))}}
function formatDate(value:string|null){if(!value)return'—';const date=new Date(value);return Number.isNaN(date.getTime())?value:date.toLocaleString('pt-BR')}
function statusLabel(status:AuditResult['status']){if(status==='ok')return'Sem divergências';if(status==='critico')return'Divergências críticas';return'Requer atenção'}
function showValue(key:string,value:unknown){const n=Number(value);if(Number.isFinite(n)&&(key.includes('valor')||key.includes('faturamento')))return currency.format(n);return String(value??'—')}
export default function CalculationAudit({adminKey,enabled}:Props){const[period,setPeriod]=useState<PeriodOption>('todo-periodo'),[customStart,setCustomStart]=useState(monthBounds(0).inicio),[customEnd,setCustomEnd]=useState(monthBounds(0).fim),[result,setResult]=useState<AuditResult|null>(null),[history,setHistory]=useState<AuditHistory[]>([]),[loading,setLoading]=useState(false),[error,setError]=useState(''),[detail,setDetail]=useState<Detail|null>(null),[detailLoading,setDetailLoading]=useState(false);const selectedPeriod=useMemo(()=>period==='mes-atual'?monthBounds(0):period==='mes-anterior'?monthBounds(-1):period==='personalizado'?{inicio:customStart,fim:customEnd}:{inicio:'',fim:''},[period,customStart,customEnd]);useEffect(()=>{if(!enabled)return;const controller=new AbortController();fetch('/api/admin/auditoria',{cache:'no-store',headers:{'x-admin-key':adminKey},signal:controller.signal}).then(async response=>{const data=await response.json() as{auditorias?:AuditHistory[];erro?:string};if(!response.ok)throw new Error(data.erro||'Não foi possível consultar as auditorias.');const audits=data.auditorias||[];setHistory(audits);if(audits[0]?.resultado)setResult(audits[0].resultado)}).catch(reason=>{if(!(reason instanceof DOMException&&reason.name==='AbortError'))setError(reason instanceof Error?reason.message:String(reason))});return()=>controller.abort()},[enabled,adminKey]);async function runAudit(){setLoading(true);setError('');setDetail(null);try{const params=new URLSearchParams();if(selectedPeriod.inicio&&selectedPeriod.fim){params.set('inicio',selectedPeriod.inicio);params.set('fim',selectedPeriod.fim)}const response=await fetch(`/api/admin/auditoria?${params}`,{method:'POST',cache:'no-store',headers:{'x-admin-key':adminKey}}),data=await response.json() as AuditResult&{erro?:string;detalhe?:string};if(!response.ok)throw new Error(data.detalhe||data.erro||'Não foi possível executar a auditoria.');setResult(data);setHistory(current=>[{id:data.id||data.executado_em,criado_em:data.executado_em,resultado:data},...current].slice(0,10))}catch(reason){setError(reason instanceof Error?reason.message:String(reason))}finally{setLoading(false)}}async function openDetail(key:DetailKey){setDetailLoading(true);setError('');try{const params=new URLSearchParams({tipo:key});if(result?.periodo.inicio&&result.periodo.fim){params.set('inicio',result.periodo.inicio);params.set('fim',result.periodo.fim)}const response=await fetch(`/api/admin/auditoria-detalhes?${params}`,{cache:'no-store',headers:{'x-admin-key':adminKey}}),data=await response.json() as Detail&{erro?:string;detalhe?:string};if(!response.ok)throw new Error(data.detalhe||data.erro||'Não foi possível abrir os detalhes.');setDetail(data)}catch(reason){setError(reason instanceof Error?reason.message:String(reason))}finally{setDetailLoading(false)}}if(!enabled)return null;const metrics=result?[['Pedidos fora da carteira',result.vinculos.pedidos_fora_carteira,'pedidos_fora_carteira'],['Pedidos sem CNPJ vinculado',result.vinculos.pedidos_sem_cnpj_vinculado,''],['Pedidos sem consultor',result.vinculos.pedidos_sem_consultor,'pedidos_sem_consultor'],['Itens sem EAN',result.vinculos.itens_sem_ean,'itens_sem_ean'],['Itens sem produto',result.vinculos.itens_sem_produto,'itens_sem_produto'],['Itens sem classificação',result.vinculos.itens_sem_classificacao,'itens_sem_classificacao'],['Datas futuras',result.qualidade.datas_futuras,'datas_futuras'],['Datas inválidas',result.qualidade.datas_invalidas,''],['Pedidos duplicados',result.qualidade.duplicatas_pedidos,''],['Itens potencialmente duplicados',result.qualidade.duplicatas_itens,''],['Valor do pedido divergente dos itens',result.qualidade.pedidos_valor_divergente,''],['Clientes oficiais sem UF',result.qualidade.clientes_sem_uf,'']] as Array<[string,number,DetailKey|'']>:[];return <section className="audit-section"><div className="audit-heading"><div><h2>Auditoria dos cálculos</h2></div>{result&&<span className={`audit-status ${result.status}`}>{statusLabel(result.status)}</span>}</div><div className="audit-controls"><label><span>Período auditado</span><select value={period} onChange={event=>setPeriod(event.target.value as PeriodOption)}><option value="todo-periodo">Todo o período extraído</option><option value="mes-atual">Mês atual</option><option value="mes-anterior">Mês anterior</option><option value="personalizado">Personalizado</option></select></label>{period==='personalizado'&&<><label><span>Data inicial</span><input type="date" value={customStart} onChange={event=>setCustomStart(event.target.value)}/></label><label><span>Data final</span><input type="date" value={customEnd} onChange={event=>setCustomEnd(event.target.value)}/></label></>}<button className="primary-action" type="button" disabled={loading} onClick={()=>void runAudit()}>{loading?'Auditando…':'Executar auditoria'}</button></div>{error&&<div className="alert alert-error">{error}</div>}{result&&<><div className="audit-reconciliation"><article><span>OL total faturado</span><strong>{currency.format(result.conciliacao.ol_total)}</strong><small>{number.format(result.volume.pedidos_faturados)} pedidos · {number.format(result.volume.itens_faturados)} itens</small></article><article><span>OL sem combate</span><strong>{currency.format(result.conciliacao.ol_sem_combate)}</strong><small>Prioritários: {currency.format(result.conciliacao.ol_prioritarios)}</small></article><article><span>OL combate</span><strong>{currency.format(result.conciliacao.ol_combate)}</strong><small>Sem classificação: {currency.format(result.conciliacao.ol_sem_classificacao)}</small></article><article><span>Diferença da conciliação</span><strong>{currency.format(result.conciliacao.diferenca)}</strong><small>Total − sem combate − combate − não classificado</small></article></div><div className="audit-metrics">{metrics.map(([label,value,key])=>key&&value>0?<button type="button" className="has-alert clickable" key={label} onClick={()=>void openDetail(key)}><span>{label}<small>Abrir detalhes</small></span><strong>{number.format(value)}</strong></button>:<article className={value>0?'has-alert':''} key={label}><span>{label}</span><strong>{number.format(value)}</strong></article>)}</div>{detailLoading&&<div className="audit-detail-loading">Carregando detalhes…</div>}{detail&&<section className="audit-detail"><div className="audit-detail-heading"><h3>{detail.titulo}</h3><button type="button" onClick={()=>setDetail(null)}>Fechar</button></div>{!detail.linhas.length?<p>Nenhum registro encontrado.</p>:<div className="audit-detail-list">{detail.linhas.map((row,index)=><article key={index}>{Object.entries(row).map(([key,value])=><div key={key}><span>{key.replaceAll('_',' ')}</span><b>{showValue(key,value)}</b></div>)}</article>)}</div>}</section>}<div className="audit-footnote"><span>{number.format(result.total_alertas)} sinais de atenção</span><span>Dados de {result.volume.primeira_data||'—'} a {result.volume.ultima_data||'—'}</span><span>Executada em {formatDate(result.executado_em)}</span></div></>}{history.length>0&&<details className="audit-history"><summary>Histórico das auditorias ({history.length})</summary>{history.map(item=><div key={item.id}><span>{formatDate(item.criado_em)}</span><strong className={item.resultado.status}>{statusLabel(item.resultado.status)}</strong><span>{number.format(item.resultado.total_alertas)} sinais</span></div>)}</details>}</section>}
