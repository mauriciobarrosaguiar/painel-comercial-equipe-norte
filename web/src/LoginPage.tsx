import{FormEvent,useState}from'react';import'./login.css';
export type SessionUser={login:string;email:string;nome:string;consultor_id:string};
export default function LoginPage({onLogin}:{onLogin:(user:SessionUser)=>void}){const[login,setLogin]=useState(''),[loading,setLoading]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({login})}),j=await r.json();if(!r.ok)throw new Error(j.detalhe||j.erro||'Acesso não autorizado.');onLogin(j.usuario)}catch(x){setError(x instanceof Error?x.message:String(x))}finally{setLoading(false)}}return <main className="login-page"><section className="login-card"><div className="login-brand"><div className="brand-mark">N</div><div><strong>Painel Comercial</strong><span>Equipe Norte</span></div></div><h1>Entre com seu login EMS</h1><form onSubmit={e=>void submit(e)}><label><span>Login ou e-mail</span><input autoFocus autoComplete="username" value={login} onChange={e=>setLogin(e.target.value)} required/></label>{error&&<div className="alert alert-error">{error}</div>}<button className="primary-action" disabled={loading}>{loading?'Entrando…':'Entrar no painel'}</button></form></section></main>}