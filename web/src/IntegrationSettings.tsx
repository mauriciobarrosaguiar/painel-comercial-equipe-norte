import{FormEvent,useEffect,useState}from'react';import BaseManagement from'./BaseManagement';import CalculationAudit from'./CalculationAudit';import TemplatesSection from'./TemplatesSection';
type IntegrationStatus={configurada:boolean;usuario_mascarado:string;status:string;mensagem:string;atualizado_em:string|null};type Props={onBack:()=>void};const formatDate=(value:string|null)=>{if(!value)return'Ainda não registrado';const date=new Date(value);return Number.isNaN(date.getTime())?value:date.toLocaleString('pt-BR')};
export default function IntegrationSettings({onBack}:Props){const[userId,setUserId]=useState(''),[accessSecret,setAccessSecret]=useState(''),[status,setStatus]=useState<IntegrationStatus|null>(null),[message,setMessage]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function request(method:'GET'|'POST'|'DELETE',body?:object){const response=await fetch('/api/admin/bussola',{method,cache:'no-store',headers:{'content-type':'application/json'},body:body?JSON.stringify(body):undefined}),data=await response.json()as IntegrationStatus&{erro?:string;detalhe?:string};if(!response.ok)throw new Error(data.detalhe||data.erro||'Não foi possível concluir a operação.');return data}async function load(){setLoading(true);try{setStatus(await request('GET'));setError('')}catch(reason){setError(reason instanceof Error?reason.message:String(reason))}finally{setLoading(false)}}useEffect(()=>{void load()},[]);async function save(event:FormEvent){event.preventDefault();setLoading(true);setError('');setMessage('');try{setStatus(await request('POST',{usuario:userId,segredo:accessSecret}));setUserId('');setAccessSecret('');setMessage('Acesso salvo e protegido com sucesso.')}catch(reason){setError(reason instanceof Error?reason.message:String(reason))}finally{setLoading(false)}}async function remove(){if(!window.confirm('Remover o acesso salvo?'))return;setLoading(true);try{await request('DELETE');setStatus({configurada:false,usuario_mascarado:'',status:'nao_configurada',mensagem:'Acesso removido.',atualizado_em:null});setMessage('Acesso removido.')}catch(reason){setError(reason instanceof Error?reason.message:String(reason))}finally{setLoading(false)}}return <main className="content admin-content"><button className="back-button" onClick={onBack}>← Voltar ao painel</button><section className="admin-hero"><span className="eyebrow">Administração</span><h1>Integrações e bases do painel</h1><p>O acesso do colaborador libera todas as configurações e atualizações.</p></section>{error&&<div className="alert alert-error">{error}</div>}{message&&<div className="alert alert-success">{message}</div>}<section className="admin-grid"><article className="admin-card"><div className="integration-heading"><div className="integration-icon">B</div><div><h2>Bússola</h2><p>Credencial técnica usada somente pela extração automatizada.</p></div><span className="status-pill">{status?.configurada?'Configurada':loading?'Carregando':'Aguardando acesso'}</span></div>{status&&<><div className="integration-status-grid"><div><span>Usuário salvo</span><strong>{status.usuario_mascarado||'Nenhum'}</strong></div><div><span>Última alteração</span><strong>{formatDate(status.atualizado_em)}</strong></div><div className="wide"><span>Situação</span><strong>{status.mensagem}</strong></div></div><form className="credentials-form" onSubmit={event=>void save(event)}><div className="form-heading"><h3>{status.configurada?'Substituir acesso técnico':'Cadastrar acesso técnico'}</h3><p>Estes dados são usados pela automação, não pelo login dos colaboradores.</p></div><label><span>Usuário do Bússola</span><input value={userId} onChange={event=>setUserId(event.target.value)} required/></label><label><span>Código de acesso do Bússola</span><input type="password" value={accessSecret} onChange={event=>setAccessSecret(event.target.value)} required/></label><div className="form-actions"><button className="primary-action" disabled={loading}>Salvar acesso</button>{status.configurada&&<button className="danger-button" type="button" onClick={()=>void remove()} disabled={loading}>Remover acesso</button>}</div></form></>}</article><aside className="security-card"><span className="security-icon">✓</span><h2>Acesso confirmado</h2><p>Não é necessário informar a antiga chave administrativa para importar bases, executar auditorias ou iniciar extrações.</p></aside></section><BaseManagement adminKey="" enabled/><TemplatesSection/><CalculationAudit adminKey="" enabled/></main>}
