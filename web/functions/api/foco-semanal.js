import { authorized, json } from '../_lib/credentials.js'
const texto=v=>String(v??'').trim(),numero=v=>Number.isFinite(Number(v))?Number(v):0,dataValida=v=>/^\d{4}-\d{2}-\d{2}$/.test(texto(v))
async function exigirAcesso(request,env){if(!(await authorized(request,env.PAINEL_ADMIN_KEY)))return json({erro:'Sessão inválida. Entre novamente no painel.'},401);return null}
function semanaAtual(){const agora=new Date(),segunda=new Date(agora);segunda.setDate(agora.getDate()-((agora.getDay()+6)%7));const domingo=new Date(segunda);domingo.setDate(segunda.getDate()+6);const iso=d=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;return{inicio:iso(segunda),fim:iso(domingo)}}
export async function onRequestGet({request,env}){try{const params=new URL(request.url).searchParams,padrao=semanaAtual(),inicio=dataValida(params.get('inicio'))?params.get('inicio'):padrao.inicio,fim=dataValida(params.get('fim'))?params.get('fim'):padrao.fim,consultor=texto(params.get('consultor')),uf=texto(params.get('uf')).toUpperCase().slice(0,2),sql=`WITH consultores_escopo AS(SELECT co.id,co.nome,COALESCE(MIN(NULLIF(TRIM(cl.setor_rep),'')),'') setor FROM consultores co LEFT JOIN clientes cl ON cl.consultor_id=co.id AND cl.carteira_importada=1 AND cl.ativo=1 WHERE co.ativo=1 AND co.origem='PAINEL_EQUIPE' AND (?='' OR co.id=?) AND (?='' OR EXISTS(SELECT 1 FROM clientes cu WHERE cu.consultor_id=co.id AND cu.carteira_importada=1 AND cu.ativo=1 AND UPPER(TRIM(COALESCE(cu.uf,'')))=?)) GROUP BY co.id,co.nome),focos_escopo AS(SELECT f.id foco_id,f.semana_inicio,f.semana_fim,COALESCE(NULLIF(f.ean,''),pr.ean,'') ean,COALESCE(NULLIF(f.descricao,''),pr.descricao,'Produto foco') descricao,COALESCE(f.observacoes,'') observacoes,f.meta_clientes meta_legada_quantidade,f.meta_valor meta_legada_valor FROM foco_semanal f LEFT JOIN produtos pr ON pr.id=f.produto_id WHERE f.ativo=1 AND DATE(f.semana_inicio)<=DATE(?) AND DATE(f.semana_fim)>=DATE(?)),metas AS(SELECT f.*,co.id consultor_id,co.nome consultor,co.setor,COALESCE(fx.meta_quantidade,CASE WHEN NOT EXISTS(SELECT 1 FROM foco_consultores fa WHERE fa.foco_id=f.foco_id AND fa.ativo=1) THEN f.meta_legada_quantidade ELSE 0 END,0) meta_quantidade,COALESCE(fx.meta_valor,CASE WHEN NOT EXISTS(SELECT 1 FROM foco_consultores fa WHERE fa.foco_id=f.foco_id AND fa.ativo=1) THEN f.meta_legada_valor ELSE 0 END,0) meta_valor FROM focos_escopo f CROSS JOIN consultores_escopo co LEFT JOIN foco_consultores fx ON fx.foco_id=f.foco_id AND fx.consultor_id=co.id AND fx.ativo=1 WHERE NOT EXISTS(SELECT 1 FROM foco_consultores fa WHERE fa.foco_id=f.foco_id AND fa.ativo=1) OR fx.consultor_id IS NOT NULL),vendas AS(SELECT cl.consultor_id,cl.id cliente_id,COALESCE(NULLIF(ip.ean,''),pr.ean,'') ean,pe.id pedido_id,DATE(COALESCE(pe.data_faturamento,pe.data_pedido)) data_venda,COALESCE(ip.quantidade_faturada,0) quantidade,COALESCE(ip.valor_faturado,0) valor FROM itens_pedido ip JOIN pedidos pe ON pe.id=ip.pedido_id LEFT JOIN produtos pr ON pr.id=ip.produto_id JOIN clientes cl ON cl.id=pe.cliente_id AND cl.carteira_importada=1 AND cl.ativo=1 WHERE ip.ativo=1 AND pe.ativo=1 AND UPPER(TRIM(COALESCE(pe.status,''))) IN('FATURADO','FATURADO PARCIAL','FATURADO RECUPERADO') AND (?='' OR UPPER(TRIM(COALESCE(cl.uf,'')))=?)) SELECT m.foco_id,m.semana_inicio,m.semana_fim,m.ean,m.descricao,m.observacoes,m.consultor_id,m.consultor,m.setor,m.meta_quantidade,m.meta_valor,COUNT(DISTINCT CASE WHEN v.valor>0 THEN v.cliente_id END) cnpj_positivados,COUNT(DISTINCT v.pedido_id) pedidos,COALESCE(SUM(v.quantidade),0) realizado_quantidade,COALESCE(SUM(v.valor),0) faturamento FROM metas m LEFT JOIN vendas v ON v.consultor_id=m.consultor_id AND v.ean=m.ean AND v.data_venda BETWEEN DATE(m.semana_inicio) AND DATE(m.semana_fim) GROUP BY m.foco_id,m.consultor_id ORDER BY m.descricao COLLATE NOCASE,m.consultor COLLATE NOCASE`,[resultado,consultores,ufs]=await env.DB.batch([env.DB.prepare(sql).bind(consultor,consultor,uf,uf,fim,inicio,uf,uf),env.DB.prepare("SELECT id,nome FROM consultores WHERE ativo=1 AND origem='PAINEL_EQUIPE' ORDER BY nome COLLATE NOCASE"),env.DB.prepare("SELECT DISTINCT UPPER(TRIM(uf)) uf FROM clientes WHERE carteira_importada=1 AND ativo=1 AND LENGTH(TRIM(COALESCE(uf,'')))=2 ORDER BY uf")]),linhas=(resultado.results||[]).map(item=>{const meta=numero(item.meta_quantidade),realizado=numero(item.realizado_quantidade);return{...item,id:`${item.foco_id}:${item.consultor_id}`,meta_quantidade:meta,meta_valor:numero(item.meta_valor),realizado_quantidade:realizado,cobertura_percentual:meta>0?realizado/meta*100:0,cnpj_positivados:numero(item.cnpj_positivados),pedidos:numero(item.pedidos),faturamento:numero(item.faturamento)}});return json({periodo:{inicio,fim},linhas,filtros:{consultores:consultores.results||[],ufs:(ufs.results||[]).map(x=>String(x.uf||'')).filter(Boolean)}})}catch(error){const detalhe=error instanceof Error?error.message:String(error);if(detalhe.includes('no such table')||detalhe.includes('no such column'))return json({periodo:semanaAtual(),linhas:[],filtros:{consultores:[],ufs:[]},aviso:'A atualização do Foco Semanal ainda não foi aplicada no banco.'});return json({erro:'Não foi possível carregar o Foco Semanal.',detalhe},500)}}
export async function onRequestPost({request,env}){const negado=await exigirAcesso(request,env);if(negado)return negado;try{const body=await request.json(),acao=texto(body.acao||'salvar'),agora=new Date().toISOString();if(acao==='excluir'){const id=texto(body.id);await env.DB.batch([env.DB.prepare('UPDATE foco_semanal SET ativo=0,atualizado_em=? WHERE id=?').bind(agora,id),env.DB.prepare('UPDATE foco_consultores SET ativo=0 WHERE foco_id=?').bind(id)]);return json({sucesso:true,id})}const inicio=texto(body.semana_inicio),fim=texto(body.semana_fim),ean=texto(body.ean).replace(/\D/g,''),descricao=texto(body.descricao),metas=Array.isArray(body.consultores)?body.consultores.filter(x=>texto(x?.id)):[];if(!dataValida(inicio)||!dataValida(fim)||inicio>fim||!ean||!metas.length)return json({erro:'Informe período, EAN e ao menos uma meta de consultor.'},400);let id=texto(body.id);if(!id){const existente=await env.DB.prepare('SELECT id FROM foco_semanal WHERE ean=? AND semana_inicio=? AND semana_fim=? LIMIT 1').bind(ean,inicio,fim).first();id=existente?.id||`foco-${crypto.randomUUID()}`}await env.DB.prepare(`INSERT INTO foco_semanal(id,semana_inicio,semana_fim,ean,descricao,meta_clientes,meta_valor,observacoes,criado_por,ativo,criado_em,atualizado_em) VALUES(?,?,?,?,?,?,?,?,'Painel',1,?,?) ON CONFLICT(id) DO UPDATE SET semana_inicio=excluded.semana_inicio,semana_fim=excluded.semana_fim,ean=excluded.ean,descricao=excluded.descricao,observacoes=excluded.observacoes,ativo=1,atualizado_em=excluded.atualizado_em`).bind(id,inicio,fim,ean,descricao||`Produto ${ean}`,0,0,texto(body.observacoes),agora,agora).run();const comandos=[env.DB.prepare('UPDATE foco_consultores SET ativo=0 WHERE foco_id=?').bind(id)];for(const meta of metas)comandos.push(env.DB.prepare(`INSERT INTO foco_consultores(foco_id,consultor_id,ativo,meta_quantidade,meta_valor) VALUES(?,?,1,?,?) ON CONFLICT(foco_id,consultor_id) DO UPDATE SET ativo=1,meta_quantidade=excluded.meta_quantidade,meta_valor=excluded.meta_valor`).bind(id,texto(meta.id),numero(meta.meta_quantidade),numero(meta.meta_valor)));await env.DB.batch(comandos);return json({sucesso:true,id,consultores:metas.length})}catch(error){return json({erro:'Não foi possível salvar o foco.',detalhe:error instanceof Error?error.message:String(error)},500)}}
