import{createSessionToken,json,sessionCookie}from'../../_lib/credentials.js'
const texto=v=>String(v??'').trim().toLowerCase(),local=v=>texto(v).split('@')[0],corporativo=v=>/^m\d{6,9}$/.test(v)
export async function onRequestPost({request,env}){try{if(typeof env.PAINEL_ADMIN_KEY!=='string'||env.PAINEL_ADMIN_KEY.length<12)return json({erro:'A chave de sessão do painel ainda não foi configurada.'},503);const body=await request.json().catch(()=>({})),informado=texto(body.login),login=local(informado);if(!login)return json({erro:'Informe seu login ou e-mail EMS.'},400);if(informado.includes('@')&&!informado.endsWith('@ems.com.br'))return json({erro:'Use o e-mail corporativo @ems.com.br.'},400);let usuario=await env.DB.prepare('SELECT login,email,nome,consultor_id FROM colaboradores_acesso WHERE ativo=1 AND (LOWER(login)=? OR LOWER(email)=? OR LOWER(email)=?) LIMIT 1').bind(login,informado,`${login}@ems.com.br`).first();if(!usuario){const encontrado=await env.DB.prepare(`SELECT co.id consultor_id,co.nome,LOWER(TRIM(MIN(CASE WHEN LOWER(TRIM(COALESCE(cl.setor_rep,''))) GLOB 'm[0-9]*' OR LOWER(TRIM(COALESCE(cl.setor_rep,''))) LIKE '%@ems.com.br' THEN cl.setor_rep END))) acesso FROM consultores co JOIN clientes cl ON cl.consultor_id=co.id AND cl.carteira_importada=1 AND cl.ativo=1 WHERE co.ativo=1 AND co.origem='PAINEL_EQUIPE' GROUP BY co.id,co.nome HAVING acesso IN (?,?,?) LIMIT 1`).bind(login,informado,`${login}@ems.com.br`).first();if(encontrado){const acesso=texto(encontrado.acesso),loginBase=local(acesso),email=acesso.includes('@')?acesso:`${loginBase}@ems.com.br`;await env.DB.prepare(`INSERT INTO colaboradores_acesso(id,login,email,nome,consultor_id,ativo,atualizado_em) VALUES(?,?,?,?,?,1,?) ON CONFLICT(login) DO UPDATE SET email=excluded.email,nome=excluded.nome,consultor_id=excluded.consultor_id,ativo=1,atualizado_em=excluded.atualizado_em`).bind(`ac-${encontrado.consultor_id}`,loginBase,email,encontrado.nome,encontrado.consultor_id,new Date().toISOString()).run();usuario={login:loginBase,email,nome:encontrado.nome,consultor_id:encontrado.consultor_id}}}if(!usuario&&corporativo(login)){const email=`${login}@ems.com.br`,nome=`Colaborador EMS ${login.toUpperCase()}`;await env.DB.prepare('INSERT OR IGNORE INTO colaboradores_acesso(id,login,email,nome,consultor_id,ativo) VALUES(?,?,?,?,NULL,1)').bind(`ac-${login}`,login,email,nome).run();usuario={login,email,nome,consultor_id:null}}if(!usuario)return json({erro:'O código informado não possui o formato de acesso EMS.'},401);const agora=new Date().toISOString();await env.DB.prepare('UPDATE colaboradores_acesso SET ultimo_acesso_em=?,atualizado_em=? WHERE login=?').bind(agora,agora,usuario.login).run();const token=await createSessionToken(usuario,env.PAINEL_ADMIN_KEY);return json({usuario:{login:usuario.login,email:usuario.email||'',nome:usuario.nome,consultor_id:usuario.consultor_id||''}},200,{'set-cookie':sessionCookie(token)})}catch(error){const detalhe=error instanceof Error?error.message:String(error);if(detalhe.includes('no such table'))return json({erro:'A atualização de acessos ainda não foi aplicada no banco.'},503);return json({erro:'Não foi possível entrar no painel.',detalhe},500)}}
