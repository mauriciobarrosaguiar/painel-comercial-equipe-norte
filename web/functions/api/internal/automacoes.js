import {authorized,json} from '../../_lib/credentials.js'
async function admin(request,env){if(typeof env.PAINEL_ADMIN_KEY!=='string'||env.PAINEL_ADMIN_KEY.length<12)return json({erro:'Chave administrativa não configurada.'},503);if(!(await authorized(request,env.PAINEL_ADMIN_KEY)))return json({erro:'Chave administrativa inválida.'},401);return null}
export async function onRequestPost({request,env}){const negado=await admin(request,env);if(negado)return negado;try{const body=await request.json().catch(()=>({})),acao=String(body.acao||'proxima').toLowerCase(),agora=new Date().toISOString();if(acao==='proxima'){await env.DB.prepare("UPDATE comandos_automacao SET status='erro',erro='Execução interrompida por tempo excedido.',finalizado_em=?,atualizado_em=? WHERE status='executando' AND datetime(iniciado_em)<datetime('now','-2 hours')").bind(agora,agora).run();const comando=await env.DB.prepare("SELECT id,tipo,parametros_json,solicitado_por FROM comandos_automacao WHERE status='aguardando' ORDER BY solicitado_em LIMIT 1").first();if(!comando)return json({comando:null});const atualizado=await env.DB.prepare("UPDATE comandos_automacao SET status='executando',iniciado_em=?,mensagem='Processamento iniciado.',atualizado_em=? WHERE id=? AND status='aguardando'").bind(agora,agora,comando.id).run();if(!atualizado.meta?.changes)return json({comando:null});let parametros={};try{parametros=JSON.parse(String(comando.parametros_json||'{}'))}catch{}return json({comando:{id:comando.id,tipo:comando.tipo,parametros,solicitado_por:comando.solicitado_por}})}if(acao==='finalizar'){const id=String(body.id||'').trim(),status=String(body.status||'').toLowerCase();if(!id||!['concluido','erro','cancelado'].includes(status))return json({erro:'Finalização inválida.'},400);await env.DB.prepare('UPDATE comandos_automacao SET status=?,mensagem=?,erro=?,finalizado_em=?,atualizado_em=? WHERE id=?').bind(status,String(body.mensagem||''),String(body.erro||''),agora,agora,id).run();return json({sucesso:true,id,status})}return json({erro:'Ação interna inválida.'},400)}catch(error){return json({erro:'Falha no processador interno.',detalhe:error instanceof Error?error.message:String(error)},500)}}
