import assert from'node:assert/strict'
import test from'node:test'
import{onRequestGet,onRequestPost}from'../functions/api/foco-semanal.js'
import{testDatabase}from'./d1-fixture.js'
const key='chave-administrativa-teste'
test('Foco Semanal calcula meta, realizado e CNPJ por consultor',async()=>{const body=await(await onRequestGet({request:new Request('https://x/api/foco-semanal?inicio=2026-07-07&fim=2026-07-13'),env:{DB:testDatabase()}})).json();assert.equal(body.linhas.length,1);assert.equal(body.linhas[0].consultor,'Ana');assert.equal(body.linhas[0].meta_quantidade,2);assert.equal(body.linhas[0].realizado_quantidade,1);assert.equal(body.linhas[0].cnpj_positivados,1);assert.equal(body.linhas[0].faturamento,100);assert.equal(body.linhas[0].cobertura_percentual,50)})
test('Foco Semanal salva metas individuais sem pedir chave na interface',async()=>{const DB=testDatabase(),env={DB,PAINEL_ADMIN_KEY:key},request=new Request('https://x/api/foco-semanal',{method:'POST',headers:{'content-type':'application/json','x-admin-key':key},body:JSON.stringify({semana_inicio:'2026-07-07',semana_fim:'2026-07-13',ean:'222',descricao:'Prioritário',consultores:[{id:'co1',meta_quantidade:3}]})}),response=await onRequestPost({request,env});assert.equal(response.status,200);const saved=await response.json();assert.equal(saved.consultores,1);const body=await(await onRequestGet({request:new Request('https://x/api/foco-semanal?inicio=2026-07-07&fim=2026-07-13'),env})).json();assert.equal(body.linhas.length,2);assert.equal(body.linhas.find(x=>x.ean==='222').meta_quantidade,3)})
