import assert from'node:assert/strict';import test from'node:test';import{onRequestPost}from'../functions/api/auth/login.js';import{testDatabase}from'./d1-fixture.js';test('novo código EMS recebe acesso completo',async()=>{const r=await onRequestPost({request:new Request('https://x/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({login:'m0099999@ems.com.br'})}),env:{DB:testDatabase(),PAINEL_ADMIN_KEY:'chave-administrativa-teste'}});assert.equal(r.status,200);assert.equal((await r.json()).usuario.login,'m0099999')})
